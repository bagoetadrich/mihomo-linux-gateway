# 迁移产物/敏感内容不入库
config.yaml
config.yaml.bak.*
Country.mmdb
GeoSite.dat
ruleset/

# 订阅源导出的配置文件（含节点凭据，严禁入库）
Clash_*.yaml
Clash_*.yml
*_sub.yaml
*_sub.yml
subscribe*.yaml
subscribe*.yml

# 本地环境变量（含面板密码等，严禁入库）
.env
.env.*
!.env.example

# 面板运行产物
data/panel/

# mihomo 运行产物（docker 挂载目录时会在宿主生成）
cache.db
*.db

# 下载的二进制/压缩包
*.gz
*.bin

# docker 数据目录(运行期自动生成的 config.yaml 等)
data/
